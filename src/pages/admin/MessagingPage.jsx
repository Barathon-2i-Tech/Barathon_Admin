import { BasicPage } from '../../components/BasicPage';
import Settings from '@mui/icons-material/Settings';

export const MessagingPage = () => {
    return <BasicPage title="Messaging Page" icon={<Settings />} />;
};
